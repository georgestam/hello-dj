<h3>All bookings:</h3>
  <% @bookings.each do |r| %>
    <div class="container">
      <div class="row">
        <div class="col-sm-6 col-lg-3 ">
          <p><%=r.date%> at <%=r.booking_address%></p>
        </div>
      </div>
    </div>
  <% end %>
